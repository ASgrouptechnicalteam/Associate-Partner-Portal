const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({include: {role: true}}); console.log(JSON.stringify(users.map(u => ({uid: u.userIdentifier, role: u.role.name})), null, 2)); } main().finally(() => prisma.$disconnect());
